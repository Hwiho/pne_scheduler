# Lab schedule fixtures (from `c:\sch.zip`)

93 `.sch` files used for batch analysis (`example/analysis/sch_zip_report.json`).

Categories include formation (FM), capacheck, 0.5C cycle, QC charge, 8-stack, etc.

```powershell
python pne_scheduler/tools/analyze_sch_zip.py c:\sch.zip
```
